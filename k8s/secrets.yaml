apiVersion: v1
kind: Secret
metadata:
  name: kraken-secrets
  namespace: kraken
type: Opaque
stringData:
  # Generate with: openssl rand -hex 32
  OPEN_KRAKEN_JWT_SECRET: "REPLACE_WITH_PRODUCTION_SECRET"
