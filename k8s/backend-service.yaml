apiVersion: v1
kind: Service
metadata:
  name: kraken-backend
  namespace: kraken
  labels:
    app: kraken-backend
spec:
  type: ClusterIP
  selector:
    app: kraken-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
