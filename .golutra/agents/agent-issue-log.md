# Agent Terminal Issue Log

## Description
- This file is the issue fact log for recent execution problems.
- Load this file before each task to review recent issue patterns and avoid repeating them.
- Add a new entry whenever execution hits a real problem such as rework, misjudgment, omission, rollback, compatibility pit, validation gap, command misuse, or unintended scope expansion.
- Each entry must stay concrete and include `Problem`, `Action`, and `Verification`.
- If no issues occur in the current task, do not add a new entry.
- Do not write long-term rules, abstractions, or one-time task summaries here; promote stable lessons to `agent-guidelines.md`.
- Use datetime format `YYYY-MM-DD HH:MM` (24h).

## Mandatory Action
- MUST: If this registration table reaches 50 entries, summarize the stable and reusable lessons into `agent-guidelines.md`, then clear only the summarized issue rows.

## Registration Table
| Time | Problem | Action | Verification |
| --- | --- | --- | --- |
| 2026-04-03 19:28 | 在 `bootstrap-migration.sh` 新增文档守护时，把 ``chat.redb`` 放进双引号 `rg` 模式，触发 shell 命令替换并输出 `command not found` 噪音，虽然脚本最终仍返回 0 | 将该 `rg` 模式改成单引号包裹，保留字面反引号匹配而不触发 shell 展开；随后复跑 bootstrap guard，确认唯一入口输出干净并继续通过 | `bash /Users/claire/IdeaProjects/open-kraken/scripts/bootstrap-migration.sh --check` 已通过，且不再出现 `chat.redb: command not found` |
| 2026-04-03 19:09 | 本轮 members 视觉系统下沉的定向渲染验证通过，但执行 `cd /Users/claire/IdeaProjects/open-kraken/web && npm run typecheck` 时暴露出既有他人模块问题：`RoadmapPage.tsx`、`terminal-runtime.ts`、`TerminalPage.tsx` 以及多处测试桩的 `ApiClient` 类型未对齐，超出本次 members 页面/样式任务边界 | 保持本轮只修改 members 真实页面入口、样式与测试，不跨边界修复 roadmap/terminal/API 类型问题；改用 `npm run build` 与 members/app-shell/role-card 的定向渲染测试证明本任务改动有效 | `npm run build` 通过；`npx tsx --test ./src/test/members-page.test.tsx ./src/test/app-shell-routes.test.ts ./src/test/role-card.render.test.tsx` 7/7 通过；`npm run typecheck` 稳定复现上述既有类型错误 |
| 2026-04-03 19:07 | 为验证聊天正式入口接线而执行仓库级 TS/route 回归时，被现有 terminal 模块阻塞：`tsc` 报 `src/pages/terminal/terminal-runtime.ts`、`src/pages/terminal/TerminalPage.tsx` 既有类型错误；包含 terminal route 的壳层测试也因 `terminal_runtime_api_incomplete` 失败 | 不跨边界修复 terminal 模块；将聊天主链路验证口径收敛为只覆盖 `/chat` 正式入口和聊天页契约的定向测试，并在 completion report 明确解除条件是 terminal 类型错误修复且 terminal route mock 满足运行时 API | `cd /Users/claire/IdeaProjects/open-kraken/web && ./node_modules/.bin/tsx --test ./src/test/chat-route-entry.test.tsx ./src/test/chat-page.test.mjs` 8/8 通过；`./node_modules/.bin/tsc -p ./tsconfig.json --noEmit` 与包含 terminal route 的回归命令稳定复现既有阻塞 |
| 2026-04-03 19:03 | 将 `verify:migration` 推进到真实 React 路由入口后，仓库现有 `test:go` 不再表现为环境阻塞，而是暴露出已有 API/OpenAPI 契约回归：`backend/go/internal/api/contracts/contracts_test.go` 断言 `terminal.delta` 缺少 `sequence` 字段 | 保持本轮只收口测试入口和浏览器自动化占位，不越界修改后端契约；将该回归作为剩余风险写入 completion report，避免误判为本轮前端测试改动引入 | `npm run test:go` 返回 exit 20，并稳定复现 `openapi missing field sequence for terminal.delta`；`npm run test:web:routes`、`npm run test:e2e:browser`、`npm run test:e2e:smoke` 通过 |
| 2026-04-03 18:59 | 仓库级 wrapper 首次联调时被共享 Go 环境脚本接口漂移阻塞：`scripts/dev/run-local.sh` 调用 `run_open_kraken_go`，`scripts/verify-go-tests.sh` 调用 `resolve_open_kraken_go_bin`/`print_open_kraken_go_env`，但 `scripts/lib/go-env.sh` 缺少这些兼容函数，导致 `dev-up` 与 `verify-all` 直接报 `command not found` | 仅在 `scripts/lib/go-env.sh` 补齐兼容函数层，统一 `resolve_open_kraken_go_bin`、`print_open_kraken_go_env`、`run_open_kraken_go` 与旧别名都走同一条去除 `GOROOT/GOTOOLDIR/GOPATH` 的仓库级 Go 调用路径，不改动业务测试逻辑 | `bash /Users/claire/IdeaProjects/open-kraken/scripts/dev-up.sh --probe` 通过；`bash /Users/claire/IdeaProjects/open-kraken/scripts/verify-all.sh` 不再报脚本接口错误，而是前进到真实 Go 测试并暴露既有后端断言失败 |
| 2026-04-03 18:58 | 将聊天契约接入正式 `ChatPage.tsx` 后，现有 `app-shell-routes.test.ts` 仍断言旧的占位类名和旧文案，首次回归时对 chat/terminal 路由产生假失败 | 不改其他成员页面实现，只把测试断言对齐到当前稳定存在的页面结构与协议文本，确保回归聚焦真实破坏而非过期快照 | `./node_modules/.bin/tsx --test ./src/test/chat-route-entry.test.tsx ./src/test/app-shell-routes.test.ts ./src/test/chat-page.test.mjs` 10/10 通过 |
| 2026-04-03 18:57 | 新增 `scripts/bootstrap-migration.sh` 首次执行时把 `rg` 事件词汇检查写成过度转义，导致 bootstrap guard 在 contract sync 阶段误报失败 | 将脚本中的事件词汇模式改成单层正则转义，并补齐 `chat.message.status.changed` 在 mock server 与 web mock client 的联动输出，避免守护脚本继续把真实漂移与脚本误报混在一起 | `bash /Users/claire/IdeaProjects/open-kraken/scripts/bootstrap-migration.sh --check` 已通过，输出 fixture shape check 与 executable steps，退出码 0 |
| 2026-04-03 18:56 | 前端应用壳任务的定向验证已通过，但执行 `cd /Users/claire/IdeaProjects/open-kraken/web && npm run test:web:unit` 时仍被现存他人模块测试阻塞：`src/test/api-client.test.mjs` 期望的事件列表未包含当前 mock client 额外发出的 `chat.message.status.changed`，不属于本次壳层/API/realtime 变更范围 | 不跨边界改写既有 mock client 事件语义；保留本任务改动，改用 `npm run typecheck`、`npm run build`、`npx tsx --test ./src/test/app-shell-routes.test.ts`、`npx tsx --test ./src/test/client-contracts.test.ts`、`npx tsx --test ./src/test/migration-web.test.tsx` 做定向验证，并把整包单测阻塞写入 completion report | `npm run typecheck` 通过；`npm run build` 通过；三个定向测试文件 10/10 通过；`npm run test:web:unit` 稳定复现 `src/test/api-client.test.mjs` 的既有事件断言失败 |
| 2026-04-03 18:52 | 按计划执行 `npm run lint` 与 `npm run test -- --test-name-pattern chat` 时，被现有他人模块问题阻塞：`tsc` 报 `src/routes/roadmap-project-data.tsx` 缺少 `../api/create-client.mjs` 声明，测试全量执行时 `roadmap-project-data.test.tsx` 因 `React is not defined` 失败，均不在本次聊天界面改动范围内 | 保留聊天界面改动不跨边界修复他人模块；补充针对本任务文件的 `node --check`、`node --test ./src/test/chat-page.test.mjs` 与 `node --test ./src/test/migration-web.test.mjs` 定向验证，区分“本任务通过”与“仓库现存阻塞” | `node --check` 通过 `ChatPage.mjs` 与 `contracts.mjs`；`node --test ./src/test/chat-page.test.mjs` 4/4 通过；`node --test ./src/test/migration-web.test.mjs` 4/4 通过；原 `npm run lint`/`npm run test -- --test-name-pattern chat` 仍复现既有阻塞 |
| 2026-04-03 18:56 | 计划里的定向验证命令沿用了旧仓库过滤习惯，但 open-kraken 当前 `web/scripts/run-tests.mjs` 只匹配 `.test.mjs` 且 `npm run test:web:unit` 直接跑全量 `tsx --test`，导致 `roadmap-project-data` 过滤命令不生效 | 不修改他人既有测试入口，改用仓库现有 `npm --prefix /Users/claire/IdeaProjects/open-kraken/web run test:web:unit` 做正式门禁，并把新分支断言集中到 `src/test/roadmap-project-data.test.tsx` | `npm --prefix /Users/claire/IdeaProjects/open-kraken/web run test:web:unit` 29/29 通过；`npm --prefix /Users/claire/IdeaProjects/open-kraken/web run typecheck` 通过；`npm --prefix /Users/claire/IdeaProjects/open-kraken/web run build` 通过 |
| 2026-04-03 19:15 | 首版 `verify-browser-smoke.mjs` 使用 `spawnSync` 直接调用 headless Chrome，导致进程阻塞且难以回收；同时把页面局部 banner 文案 `Ready` 当作硬门禁，浏览器端时序上不稳定 | 改成和仓库既有浏览器脚本一致的异步 `spawn` 执行模型，并把浏览器 smoke 的断言收敛为正式路由落点、导航激活、保存按钮与全局 notices 这些稳定页面契约 | `npm --prefix /Users/claire/IdeaProjects/open-kraken run test:e2e:browser` 通过，输出 `[verify-browser-smoke] roadmap route rendered via http://127.0.0.1:4174/roadmap` |
| 2026-04-03 18:49 | 部署与运行时验证阶段再次被本机 Go toolchain/GOROOT 版本不一致阻塞，`go test ./...` 与 `scripts/dev/run-local.sh --probe` 都在标准库编译前失败，无法用当前环境完成后端启动验证 | 继续完成 open-kraken 侧的运行时配置、健康检查、静态托管、日志与脚本语义落地，并把该问题作为环境级 blocker 写入文档与 completion report，避免误判为代码回归 | 已验证 `cd /Users/claire/IdeaProjects/open-kraken/web && npm run build` 通过；`cd /Users/claire/IdeaProjects/open-kraken/backend/go && go test ./...` 与 `bash /Users/claire/IdeaProjects/open-kraken/scripts/dev/run-local.sh --probe` 稳定返回 `compile: version "go1.25.3" does not match go tool version "go1.25.6"`；退出后确认无 `backend.pid` 且 8080 无监听进程 |
| 2026-04-03 17:41 | `go test ./...` 在仓库断言运行前即被本机 Go toolchain/GOROOT 版本不一致阻塞，无法直接用当前环境证明新增测试是否通过 | 在门禁脚本与文档中单独引入 “blocked by existing environment/toolchain issue” 分类，并用匹配到的错误模式区分环境阻塞与代码回归 | 本轮执行 `cd /Users/claire/IdeaProjects/open-kraken/backend/go && go test ./...` 返回 `compile: version "go1.25.3" does not match go tool version "go1.25.6"`，后续脚本验证也将其归类为 blocked |
| 2026-04-03 19:24 | Go toolchain 主链路收口后，`bash ./scripts/verify-go-tests.sh layers` 与 `workspace` 不再被 `go1.25.3/go1.25.6` 环境错配阻塞，而是前进到真实仓库回归：`internal/api/contracts/contracts_test.go` 断言缺少 `chat.message.created` OpenAPI schema 片段 | 保持本轮工作聚焦在 toolchain bootstrap/检测/报错与统一入口，不跨边界修复 API 契约实现；把该失败作为“真实 toolchain 下已验证的现有回归”写入 completion report，便于 API 契约负责人继续收口 | `bash /Users/claire/IdeaProjects/open-kraken/scripts/verify-go-tests.sh layers` 返回 20，`bash /Users/claire/IdeaProjects/open-kraken/scripts/verify-go-tests.sh workspace` 返回 50，错误稳定定位到 `backend/go/internal/api/contracts/contracts_test.go` |
